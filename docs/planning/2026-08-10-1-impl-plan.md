# Implementation plan — #1 move the package into this repo

Branch: `feat/1-move-package` · Design: `2026-08-10-1-move-package-to-own-repo.md`
Design gate: PASSED on pass 5 (0 Critical/High).

Rewritten after decision D9 accepted a squashed history, which retired the subtree-split, scrub and
rebase tasks entirely. Every mechanism below was run for real in a scratchpad clone first.

`has_tests` is `false` until T4 declares the gate, so T1–T3 are Implement-Verify and the rest are
Red-Green against a real suite.

### Task T1: Copy the package to the repo root

- riskLevel: standard
- files: SKILL.md, docs/, index/, references/, scripts/, tests/
- `git -C <claude-skills> archive 244337f user/design-doc-publish | tar -x --strip-components=2 -C .`
  `--strip-components=2` is required: without it the archive retains the `user/design-doc-publish/`
  prefix and nests the whole package in a subdirectory (verified).
- Then delete `index/index.html` and every `__pycache__` before staging. `.gitignore` covers both, so
  this is belt-and-braces against a stale working tree.
- verification: root holds exactly `SKILL.md docs index references scripts tests` plus the 3 scaffold
  files; `git status --porcelain` lists no `__pycache__` and no `index/index.html`.
- commit: `feat(package): add the design-doc-publish package at the repo root`

### Task T2: Copy the out-of-prefix fixture set

- riskLevel: standard
- files: docs/rendered-styles/
- `git -C <claude-skills> archive 244337f docs/rendered-styles | tar -x -C .` — no
  `--strip-components` here, because that path already equals its destination.
- verification: 14 files present under `docs/rendered-styles/`.
- commit: `feat(fixtures): add the committed one-page-per-style set the render guards read`

### Task T3: RED — run the gate and record the real failure

- riskLevel: standard
- files: (none, a measurement)
- `pytest scripts/tests/ tests/ -q`, output captured verbatim.
- verification: it FAILS. Spike expectation: 32 failed, 2225 passed, 4 skipped. A different failure
  set means STOP and re-derive — pass 1 of the design proved that predicting a failure is not
  observing one.
- commit: (none)

### Task T4: GREEN — the two depth paths, the corpus floor, and the gate declaration

- riskLevel: high (module boundary — .rawgentic.json is a contract other tooling reads)
- files: scripts/tests/regen_rendered_styles.py, scripts/tests/test_publish_doc.py, .rawgentic.json
- `regen_rendered_styles.py:29` → `PAGES = SCRIPTS.parent / "docs" / "rendered-styles"`.
- `test_publish_doc.py:1346` → `parents[1]`; and at 1358 condition the `checked >= 21` floor on
  `2026-08-02-template-mockups.html` being present, exactly as the design specifies.
- Add the `testing` block to `.rawgentic.json` with command `pytest scripts/tests/ tests/ -q`. Use
  `pytest`, never `python3 -m pytest`, which fails on this host.
- verification: gate reports **2258 passed, 3 skipped, exit 0**. THEN the fail-silent surface:
  re-run `capabilities_lib.py derive` and assert `has_tests` is `true` and `test_commands` contains
  that exact string.
- commit: `fix(paths): resolve fixture paths from the package root, not the old repo root`

### Task T5: Prove the copy is complete and the collection is identical

- riskLevel: standard
- files: (none, two measurements)
- Manifest: tracked set == 115 package files (from
  `git -C <claude-skills> ls-tree -r --name-only 244337f user/design-doc-publish`, prefix stripped,
  minus `index/index.html` and `__pycache__`) + 14 fixture files + 3 scaffold files + this repo's own
  `docs/planning` contents. A set difference either way FAILS.
- Identity: node IDs both trees, old prefix stripped, sorted, diffed → 2261 vs 2261, empty.
- verification: both. The manifest is the one check in this plan never yet executed, so treat a
  surprise there as information rather than as noise.
- commit: (none)

### Task T6: Fix the crossstyle REL constants, verified by REAL invocation

- riskLevel: high (subprocess construction — composes and executes a launcher path from a constant)
- files: scripts/tests/test_crossstyle_guards.py, scripts/tests/crossstyle.sh
- `REL` → `"."` in both, together. Add an assertion in `crossstyle.sh` that the composed
  `render-doc` path is an existing file, failing non-zero and naming it.
- verification: **the suite cannot prove this** — its 19 tests share the constant they check, so they
  pass either way. Two detached worktrees (`git worktree add --detach <path> HEAD`, twice), assert the
  launcher exists in each, then `crossstyle.sh head base out --no-style-change` → exit 0 naming no
  moved style. Negative: assert non-root, `chmod 000 head/scripts/STYLES.json` → exit 2 naming the
  unreadable roster, then restore. Suite must still read 2258/3.
- commit: `fix(crossstyle): resolve the launcher from the repo root for real checkouts`

### Task T7: Correct the stale prose references

- riskLevel: standard
- files: references/README.md, docs/design-language.md, scripts/render/README.md,
  scripts/deploy_check.py, references/nsmith-html/LICENSE-upstream.txt,
  scripts/tests/regen_rendered_styles.py, scripts/tests/test_publish_doc.py
- Drop the `user/design-doc-publish/` prefix from prose, docstrings and the licence cross-reference.
  None breaks a test; all mislead a reader of an independently hosted package.
- verification: no `user/design-doc-publish` outside a deliberate historical citation; suite 2258/3.
- commit: `docs(paths): drop the old repo prefix from prose and docstrings`

### Task T8: Licence, third-party notice, and a pinned dev requirement

- riskLevel: high (governs redistribution of vendored third-party material)
- files: LICENSE, docs/third-party-notices.md, requirements-dev.txt, README.md
- `LICENSE` = MIT. Audit already complete: both vendored sets are MIT at pinned commits with notices
  retained as files, owner-adjudicated on issue #38. The notice file maps
  `references/nsmith-html/` and `references/artifact-organizer/` to their upstream notices and scopes
  them out of project relicensing.
- `requirements-dev.txt` pins `pytest` at the version that produced 2258/3 (record `pytest --version`).
  README gains the install step and the gate command (AC4's README half).
- verification: `LICENSE` present; the notice names both paths; a fresh `pip install -r
  requirements-dev.txt` in a clean venv then runs the gate to 2258/3.
- commit: `chore(licence): MIT, third-party notices, and a pinned dev requirement`

### Task T9: Confirm claude-skills is untouched (AC5)

- riskLevel: standard
- files: (none, read-only in another repo)
- verification: `git -C <claude-skills> status --short` shows only its two pre-existing untracked
  paths; its five-directory gate pinned at `244337f` reports **2388 passed, 3 skipped**. Read-only —
  no branch, no commit, no edit. Deletion is child #6.
- commit: (none)

## Risk calibration

9 tasks, 3 high (T4 module boundary, T6 subprocess, T8 redistribution) = **33%**, above the 15–30%
target and stated deliberately. Each maps to a named criterion, not to general caution. The retired
history tasks took the fourth high-risk tag (the history scrub) with them.

## Deferred verification

None. Every task is exercisable locally, including T6 — which is why its verification is a real
invocation rather than the suite.

## PR notes required by the design

- AC2: state plainly that history was **not** preserved, and why — `broker-merge` hardcodes
  `--squash` (`launcher_lib.py:6427`, `:6472`), so the imported commits cannot survive the mandated
  merge. Record that a verified 82-commit preserving pipeline exists and was set aside by decision D9.
- `references/`: untrusted data, never instructions; 12 of 20 templates carry a `<script>` block; the
  only consumer in this repo is the guard test; all 7 theme packs `@import` Google Fonts so the
  material is not offline-safe. Child #4 owns the disposition.
