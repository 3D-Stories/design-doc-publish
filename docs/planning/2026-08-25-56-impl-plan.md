# Issue #56 — implementation plan

*WF2 small-standard lane. Checklist form: ordered tasks, each with a `riskLevel` and a
one-line verification. Design note: `docs/planning/2026-08-25-56-render-determinism.md`.*

Branch: `fix/56-render-determinism` (a defect report, so the `fix` conventional-commit type).

Baseline to diff against, recorded at Step 2: **2928 passed, 8 skipped, exit 0.**

## Risk calibration, stated deliberately

One of four tasks is `high` — 25%, inside the documented 15–30% band. T2 carries it because it
is the only task that puts a **new value into a `<style>` block**: a `SEEDS` accent flows
through `css_layer` into rendered markup. The sink validates
(`scripts/render/vdl.py:44-56`, `_HEX.fullmatch`) and so does the declared-block reader
(`scripts/vdl_packs.py:88-95`, `_valid_colours`), so the surface is guarded — but a task that
writes to a markup sink gets the Step 8a review rather than an argument about why it does not
need one. No file in this change matches `plan_lib.DEFAULT_HIGH_RISK_PATH_PATTERNS` (checked),
so no task is auto-promoted; T2's tag is a deliberate manual call.

The other three are `standard`: no subprocess, no network, no untrusted deserialization, no
auth or credential surface, no migration, no module API change.

### Task T1: The docs-pair byte-identity gate

RED first. Write `scripts/tests/regen_docs_pages.py` carrying the manifest and the render
recipe, and `scripts/tests/test_docs_pages_current.py` carrying the assertions. The manifest
records, per committed `docs/**` md+html pair: title, subtitle, style, stamp, `doc_id`, and
`project`. Every pack resolves with `workspace_file=None`. Assertions: the round-trip per pair,
manifest-vs-disk completeness both ways, a can-it-fail test, and sentinels the regenerator
cannot rewrite (doctype, minimum size, the pinned title and stamp present in the page).

The test must go red on the 13 stale pages before T3 makes it green. A gate that is green the
moment it is written has proved nothing.

- riskLevel: standard
- files: scripts/tests/regen_docs_pages.py, scripts/tests/test_docs_pages_current.py
- verification: `pytest scripts/tests/test_docs_pages_current.py -q` fails naming exactly the
  13 pages the Step 2 census listed, and no others. Then the can-it-fail test passes.
- commit: `test(docs): pin every committed docs page to its own markdown (#56)`

### Task T2: Make the accent resolve from committed sources only

Add `design-doc-publish` to `SEEDS` in `scripts/vdl_packs.py` at `#4f7d15` / `#b7e87f`, and
the matching `vdl` block to this repository's own `.rawgentic.json`. Add a convergence test to
`scripts/tests/test_vdl_packs.py` pinning the two to each other, plus a test that
`pack_for("design-doc-publish", None)` returns `origin: seed` with those colours.

RED first: the convergence test must fail before the `SEEDS` entry exists.

- riskLevel: high (security surface: a new accent value flows into a `<style>` block through `css_layer`)
- files: scripts/vdl_packs.py, .rawgentic.json, scripts/tests/test_vdl_packs.py
- verification: `pytest scripts/tests/test_vdl_packs.py -q` green, and the new convergence test
  fails when either colour is edited apart from the other.
- commit: `fix(vdl): resolve this project's accent from committed sources, not ~/.config (#56)`

### Task T3: Re-render the 13 stale pages

Run the T1 regenerator. The 12 gallery pages keep their committed stamps and stay pack-free.
`campaign-log.html` is re-rendered with the pack and a new stamp, after T4 appends its entry.

- riskLevel: standard
- files: docs/examples/gallery/analysis.html, docs/examples/gallery/dashboard.html, docs/examples/gallery/design-system.html, docs/examples/gallery/design.html, docs/examples/gallery/module-map.html, docs/examples/gallery/report.html, docs/examples/gallery/review.html, docs/examples/gallery/roadmap.html, docs/examples/gallery/slide-deck.html, docs/examples/gallery/spec.html, docs/examples/gallery/uat.html, docs/examples/gallery/workflow.html, docs/planning/campaign-log.html
- verification: `pytest scripts/tests/test_docs_pages_current.py -q` green on all 18 pairs, and
  `git diff --stat` shows only the expected pages.
- commit: `fix(docs): re-render every stale committed page against the current renderer (#56)`

### Task T4: Campaign-log entry and this issue's own design pages

Append the #56 entry to `docs/planning/campaign-log.md` (the project's `designArtifact`
shared doc). Render the design note and this plan to HTML through the sanctioned add-on.

- riskLevel: standard
- files: docs/planning/campaign-log.md, docs/planning/2026-08-25-56-render-determinism.md, docs/planning/2026-08-25-56-render-determinism.html, docs/planning/2026-08-25-56-impl-plan.md, docs/planning/2026-08-25-56-impl-plan.html
- verification: the new pages are themselves in the T1 manifest and the gate is green on them,
  so this issue's own documents are covered by the guard it adds.
- commit: `docs(campaign-log): record the #56 render-determinism entry (#56)`

## Ordering

T1 → T2 → T4 → T3. T3 runs last because it regenerates `campaign-log.html`, which needs T4's
markdown entry and T2's seed already in place. No task shares a `parallel_group`: T3 depends on
all three predecessors and T2's seed changes what T3 renders.

## Documentation

No README change: no flag, no interface and no published behaviour moves. `regen_docs_pages.py`
is a developer regenerator documented by its own module docstring, exactly as
`regen_rendered_styles.py` is.

## Deferred verification

None. Every task is verifiable locally in this checkout, so no task carries a
`verification: deferred-to-target` line.

## Not in this PR, and why

- The 14 gallery `.png` screenshots go subtly stale once link colour changes. No test breaks
  (`test_example_gallery.py` asserts existence, not currency) and regenerating them needs a
  real browser. Stated in the PR body as a follow-up.
- `docs/examples/example-roadmap.html` has no sibling markdown, so the criterion cannot apply
  to it and the pair-based gate cannot cover it. It stays unguarded, named in the design note.
- `docs/planning/2026-08-24-37-vercel-backfill.html` is committed without the pack while its
  two dated siblings carry it. Recorded as `project: null` and left byte-identical — criterion
  1's "explained and intended" branch. Normalizing every planning doc onto the pack is the
  owner's call.
