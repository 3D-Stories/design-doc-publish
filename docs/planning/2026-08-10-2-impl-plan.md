# Implementation plan — #2 package as an installable plugin

Design: `docs/planning/2026-08-10-2-package-as-plugin.md` (including its post-gate amendments) ·
Base `e733007` · Baseline `2257 passed, 4 skipped, exit 0`

Verification strategy: red before green per task wherever a test can express the change. The guard
in T6 is written FIRST and fails until T1 and T2 land. Scoped suites during iteration. The full
suite runs once at Step 9, diffed against the baseline above.

| # | Task | riskLevel | Verification |
| --- | --- | --- | --- |
| T6 | New guard `scripts/tests/test_plugin_packaging.py`, written red: manifest carries the eight fields, marketplace lists the skill, `SKILL.md` uses `${CLAUDE_PLUGIN_ROOT}`, no `~/.claude/skills/`-rooted path survives, no unlicensed vendored set present, zero dangling `nsmith-html` references outside the notices | medium | the guard, red first then green |
| T1 | `.claude-plugin/plugin.json` (concrete content in design item 11) and `.claude-plugin/marketplace.json` (`source: "./"`, `strict: true`, `skills: ["./skills/design-doc-publish"]`) | medium | T6 |
| T2 | Move `SKILL.md` → `skills/design-doc-publish/SKILL.md`. Rewrite lines 12, 42, 81 to `${CLAUDE_PLUGIN_ROOT}/` | **high** | T6 |
| T3 | Repoint four SKILL.md locators: `test_style_reachability.py:29`, `test_chip_vocabulary_is_documented.py:38`, `test_publish_doc.py:927`, `:1230` | low | those suites, scoped |
| T4 | Rewrite three install-shape assertions in `test_publish_doc.py` (`:1084`, `:1088`, `:1092`). The last pinned the checkout directory NAME — replace with an existence check relative to the package root | medium | `pytest scripts/tests/test_publish_doc.py` |
| T5 | Delete `references/nsmith-html/` (20 templates + `LICENSE-upstream.txt`). Update `tests/test_vendored_references.py`, `references/README.md`, `references/manifest.json`, and the nine `scripts/render/templates/*.py` comments. Record the removal and pinned upstream commit in `docs/third-party-notices.md` | **high** | `pytest tests/`, plus T6's referential-integrity assertion |
| T7 | Remove `docs/vercel-account.md` from this repo (owner decision D13 — it documents one account, not the tool) | medium | T6 asserts it is absent from the shipped tree |
| T10 | `README.md` install section: `claude plugin marketplace add` then `claude plugin install`, in that order. The human-facing command must NOT use `${CLAUDE_PLUGIN_ROOT}`, which no shell expands | low | scoped doc guards |
| T11 | Live verification. Install the real plugin, assert the resolved skill root is under `~/.claude/plugins/cache/` and NOT the legacy symlink, render one real `.md` end to end through the installed copy with all 7 stages OK, confirm a genuinely fresh session can invoke it, then uninstall AND remove the orphaned version directory, asserting both settings and cache path are gone | **high** | AC3, AC4, AC6 — evidence captured verbatim |

Three tasks are `riskLevel: high` (T2, T5, T11), so **Step 8a per-task review is mandatory** as one
accumulated wave over their commits.

## Deferred verification

None planned. T11 is the live check and runs in-run. If the fresh-session half (AC3) cannot be
completed from inside this session, it is recorded as a deferral carrying its local proxy and its
target check, never silently dropped.

## Sequencing

T6 (red) → T1 → T2 → T3 → T4 → T5 → T7 → T10 → T11.
T3 and T4 both edit `test_publish_doc.py`, so they run adjacent and in that order.

## Scope note

T7 removes `docs/vercel-account.md`, which stays in #2 because it is about what the plugin ships.

The setup flow does NOT ship here. Owner decision D14 split it into its own child,
[#9](https://github.com/3D-Stories/design-doc-publish/issues/9), which now carries the setup entry
point and the retirement of the hardcoded `~/rawgentic/.rawgentic_workspace.json` default at
`scripts/publish_doc.py:134` and `index/build_index.py:75`. #9 depends on #2 and is in this run's
queue. So #2 stays inside its six original acceptance criteria.
